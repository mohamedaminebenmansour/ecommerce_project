import { FormFieldValidators } from './form-field-validators';

describe('FormFieldValidators', () => {
  it('should create an instance', () => {
    expect(new FormFieldValidators()).toBeTruthy();
  });
});
